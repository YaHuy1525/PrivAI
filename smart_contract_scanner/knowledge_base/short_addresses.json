{
    "description": "A vulnerability where the EVM pads short addresses with zeros, which can be exploited in some cases.",
    "snippets": [
        {
            "file": "short_address_example.sol",
            "pragma": "0.4.11",
            "code": "     function sendCoin(address to, uint amount) returns(bool sufficient) {\n"
        }
    ]
}